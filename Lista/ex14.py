def somar_todos(*nums):
    return
def mostrar_dados(**dados):
    return
#*args são argumentos posicionais , reune os valores em um tupla , permite passar quantos valores quiser
#**kwargs são argumentos flexiveis , reue tudo em um unico dicionário
# diferença : **kwargs são mais especificos do que *args